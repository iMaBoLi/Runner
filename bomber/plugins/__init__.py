from telethon import Button

main_menu = [
    [Button.text(text="اطلاعات من 📝", resize=True), Button.text(text='Make Money', resize=True)],
    [Button.text(text='Make Money', resize=True), Button.text(text='Make Money', resize=True)],
]
